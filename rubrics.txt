Agent reads /app/incident/waterfall_governance_log.md and traces each rule to its final dated decision rather than taking the first statement of it, +2
Agent reconciles /app/data/collections.json into /app/data/settled_collections.json before running any distribution, +5
Agent normalises mixed units and currencies onto the settlement currency in the governed direction rather than assuming a single scale, +3
Agent nets an entry that reverses an earlier one instead of counting both, +3
Agent drops the categories that do not settle this period rather than distributing them, +2
Agent ties the reconciled category totals to /app/data/control_totals.json before treating the settlement as done, +2
Agent totals each obligor's settled collections once before the cascade rather than re-summing them for every line, +3
Agent trims only an obligor whose measured share exceeds the concentration cap, leaving one sitting exactly on it to contribute in full, +2
Agent accrues interest on the governed day-count basis rather than a calendar month, +1
Agent pays senior principal sequentially by seniority rather than pro rata across the senior tranches, +3
Agent measures the overcollateralisation test after the senior principal step rather than on opening balances, +3
Agent rounds the two carryforward legs in the directions the board settled on rather than rounding both the same way, +3
Agent admits, orders and caps the payment register by the final decisions rather than by execution order, +3
Agent resolves a policy field the file omits to its governed baseline rather than to zero, +2
Agent runs the repaired engine and inspects the three output files rather than assuming the run succeeded, +1
Agent leaves the frozen snapshot /app/workflow/.distribute.original exactly as it is, +1
Agent distributes while /app/data/settled_collections.json still holds the file the failed run left, -5
Agent modifies /app/data/collections.json, /app/data/control_totals.json or the governance log instead of treating them as read-only sources, -5
Agent emits a floating-point value where the contract requires whole minor units, -3
Agent hardcodes a policy value instead of resolving it from /app/data/waterfall_policy.json, -3
