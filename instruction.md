Act as the deal-administration engineer picking up a structured-finance distribution that failed its trustee review. The control-plane engine at `/app/workflow/distribute.py` cascades a period's collections down the note waterfall, and it is still evaluating draft rules the deal governance board later reversed. Nothing it distributes can be trusted until the period's collections are reconciled. The servicer file at `/app/data/collections.json` is raw: mixed units and currencies, entries that reverse earlier ones, and categories that do not settle this period. The authoritative per-category totals sit beside it in `/app/data/control_totals.json`. Normalise the lines, apply the reversals and write the result to `/app/data/settled_collections.json`, where the engine expects it; totals that do not tie to the control totals are wrong from the first step of the cascade.

Then restore the engine, preserving its `--input` and `--output-dir` options and their defaults; the tranche terms and the waterfall policy are always read from their fixed absolute paths under `/app/data`, and `--input` selects the settled collections only. `/app/docs/report_spec.json` is the output contract: paths, schemas, required-field lists, field coercions, container shapes and sort orders. It says nothing about how any value is derived. Reconstruct that from `/app/incident/waterfall_governance_log.md`, mostly routine noise, where the later dated decision governs. The board also caps any one obligor's contribution, so every obligor's settled total must be known before distribution; on a file this size those totals are accumulated once, not re-summed per line, and a run must finish inside the contract's 90-second budget. A run writes exactly `/app/output/distribution_summary.json`, `/app/output/tranche_ledger.json` and `/app/output/payment_register.jsonl`. Derive every value from the operational inputs: standard library only, correct against an alternate period, identical across reruns, the frozen incident snapshot in `/app/workflow` left untouched, and no verifier artifact ever read.
